<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.king.framework.dataDictionary.mapper.DataDictionaryMapper">

    <select id="selectByDataType" parameterType="string" resultType="com.king.framework.dataDictionary.entity.DataDictionary">
        SELECT 
            DICTIONARY_ID,
            DATA_TYPE,
            DATA_NAME,
            DATA_VALUE,
            POSITION_NO,
            STATUS,
            COMMENT
        FROM data_dictionary
        WHERE DATA_TYPE = #{dataType}
        AND STATUS = 'A'
        ORDER BY POSITION_NO ASC, DICTIONARY_ID ASC
    </select>

</mapper>
